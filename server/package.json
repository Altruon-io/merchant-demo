{
  "name": "altruon-merchant-demo-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Express backend for the Altruon Merchant Demo. Holds the Altruon secret key and proxies session / estimate / transaction calls so the key never reaches the browser.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
